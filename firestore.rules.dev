rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 開発用の緩いルール - 本番環境では使用しないでください
    // すべてのドキュメントに対して読み書きを許可（認証不要）
    match /{document=**} {
      allow read, write: if true;
    }
  }
}